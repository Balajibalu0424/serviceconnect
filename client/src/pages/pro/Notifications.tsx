// Pro notification page — reuses the shared Notifications component which is already role-aware.
// It checks user.role and adjusts all deep-link routing for PROFESSIONAL users automatically.
export { default } from "@/pages/customer/Notifications";
